function breed(user,startPop,goal){
	this.user = user;
	this.startPop = startPop;
	this.goal = goal;
	
	var population=[];
	for(i=0,i<population.length,i++){
		population[i]= startPop.split(",")
	}
}
